# Copy to .env — Expo exposes EXPO_PUBLIC_* to the app automatically.
EXPO_PUBLIC_SUPABASE_URL=https://<project>.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=<anon key>

# Server-side only (set via `supabase secrets set` for the sharpen edge function):
# ANTHROPIC_API_KEY=sk-ant-...
